---
import Layout from '../../layouts/Layout.astro';
import { readMarkdownFile } from '../../lib/content';

const content = readMarkdownFile('year-10-digital-tech/assessment-calendar-2026.md');
---

<Layout title={content?.title ?? 'Assessment Calendar'}>
  <main class="container">
    {content ? <article class="prose" set:html={content.html} /> : <p>Content not found.</p>}
  </main>
</Layout>
